# requirements.txt — Install with: pip install -r requirements.txt

# ── API Framework ─────────────────────────────────────────────────
fastapi==0.111.0          # The web framework
uvicorn[standard]==0.29.0 # ASGI server that runs FastAPI
python-multipart==0.0.9   # Required for file uploads (UploadFile)

# ── Database ──────────────────────────────────────────────────────
sqlalchemy==2.0.29        # ORM — maps Python classes to DB tables
# For production PostgreSQL, also add:
# psycopg2-binary==2.9.9

# ── Data Validation ───────────────────────────────────────────────
pydantic==2.7.1           # Used by FastAPI for request/response schemas

# ── AI / Computer Vision ─────────────────────────────────────────
opencv-python==4.9.0.80
mediapipe==0.10.33
numpy==1.26.4
scipy==1.13.0

# ── Utilities ────────────────────────────────────────────────────
python-dotenv==1.0.1      # Load .env file for config